hit_target — expert controller demos

default  : blocker_enabled=false, blocker_dynamic=false  (moving target only)
opt1     : blocker_enabled=true,  blocker_dynamic=false  (green static blocker)
opt2     : blocker_enabled=false, blocker_dynamic=true   (red moving blocker)
opt1+2   : blocker_enabled=true,  blocker_dynamic=true   (green static + red dynamic;
           red sits dual_blocker_gap=1cm in front of green)

Blocker–target gap (per episode, Uniform):
  Opt1 / Opt2 : blocker_y_gap ~ U[0.050, 0.080] (5–8 cm)
  Opt1+2      : static uses same sample; dynamic + dual_blocker_gap=0.010 m in front

Geometry:
  Stick = original size (shaft [0.045,0.008,0.008], tip [0.012,0.004,0.004]).
  Target unchanged (board 0.075 / center 0.018).
  Blocker radius 0.028 (30% smaller diameter than 0.040).
  Sway amp 0.243, motion_x ±0.272 (+0.10 m outer vs 0.143 / ±0.172).
  Target + dynamic blocker travel only on the stick's half (mirror about x=0):
    right → x∈[0, span]; left → x∈[−span, 0]. Static green stays at midline.
  Blocker speed base matches target at 0.65 (shared per-ep jitter).
  Arm side: random left/right each episode (exact mirror about table midline x=0).
    left → stick on −x + left arm; right → stick on +x + right arm.
    Override with arm_side=left|right.

Success: tip sticks in yellow center AND never contacts a blocker.

Expert suite: default 5/5, opt1 5/5, opt2 5/5, opt1+2 5/5 (see test_report.json).

Files (side-by-side is the primary deliverable):
  default_sidebyside.mp4   opt1_sidebyside.mp4
  opt2_sidebyside.mp4      opt1+2_sidebyside.mp4
Also: <tag>_head.mp4, <tag>_topdown.mp4
