Follow the basic rules. When adding objects, the obejects should not collide or overlap. Respect the physics rules, objects should not appear or disappear. the round objects should roll and others slip if they are tilted on surfaces. The robot actions should be proper. If the robot is grasping an object, the gripper fingers should not close inside of it, rather around it. the objects should not be attached to the gripper, rather should be properly grasped by the gripper.